import React from 'react';
import AttendanceDashboard from '../admin/AttendanceDashboard';

const FacultyAttendance = () => {
    return (
        <AttendanceDashboard readOnly={true} />
    );
};

export default FacultyAttendance;
